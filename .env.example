# OpenAI API 키 (반드시 필요)
OPENAI_API_KEY = #API 키를 여기에 붙여넣기

# SQLite DB 파일 경로 (없으면 기본값 image_log.db)
DB_PATH=./app.db

# (선택) 모델명 (안 쓰면 gpt-4o-mini 사용)
VISION_MODEL=gpt-4o-mini

# (선택) 프롬프트 기본값
VISION_PROMPT=이 이미지를 간단히 설명해줘. 핵심 특징만 한국어로 요약.

# 환경변수저장 : DB연결정보(host, username, pw, port..), API Key 등